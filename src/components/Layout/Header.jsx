import { Fragment } from "react";

const Header = (props) => {
  return (
    <Fragment>
      <header>
        <h1>ReactMeals</h1>
        <button>Cart</button>
      </header>
    </Fragment>
  );
};

export default Header;
